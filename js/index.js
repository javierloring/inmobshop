/**
 * Gestiona los anuncios de nivel de resalto 5, que aparecen en portada de la web
 * @param  {element} element elemento receptor de la portada
 * @return {[type]}         [description]
 */
function colocar_portada(element){
    //obtenemos el listado de anuncios de nivel 5 consultando al abase de datos
    $.ajax()
    //cada anuncio de nivel 5 estará un día en portada
    const tiempo = 1000*3600*24;
    setTimeout(function(){

    }, tiempo);
}
